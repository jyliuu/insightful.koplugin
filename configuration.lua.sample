-- Copy this file to configuration.lua. That file is ignored by Git because it
-- contains a secret.
return {
    -- This provider is used until another configured provider is selected in
    -- Insightful's settings menu.
    provider = "openai",

    -- These settings are shared by every provider profile below.
    timeout = 60,
    stream = true,
    verify_ssl = true,

    -- The settings menu lists profiles whose api_key is not empty. Keep each
    -- endpoint, model, key, and provider-specific option in its profile.
    providers = {
        openai = {
            base_url = "https://api.openai.com/v1/chat/completions",
            model = "gpt-4.1-mini",
            api_key = "",
        },
        deepseek = {
            base_url = "https://api.deepseek.com/chat/completions",
            model = "deepseek-chat",
            api_key = "",
        },
        openrouter = {
            base_url = "https://openrouter.ai/api/v1/chat/completions",
            model = "openrouter/auto",
            api_key = "",
        },
        anthropic = {
            base_url = "https://api.anthropic.com/v1/messages",
            model = "claude-sonnet-4-5",
            api_key = "",
            -- max_tokens = 8192,
        },
    },
}
